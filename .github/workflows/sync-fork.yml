name: Sync OSC Fork

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        default: "prod"
        type: choice
        options:
          - prod
          - dev
          - stage
  workflow_call:
    inputs:
      environment:
        description: "Target environment"
        required: true
        type: string

jobs:
  sync-fork:
    name: Sync fork (${{ inputs.environment }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    concurrency:
      group: sync-fork-${{ inputs.environment }}
      cancel-in-progress: false
    steps:
      - name: Validate environment
        run: |
          case "${{ inputs.environment }}" in
            prod|dev|stage) ;;
            *) echo "Invalid environment: ${{ inputs.environment }}"; exit 1 ;;
          esac

      - name: Install OSC CLI
        run: npm install -g @osaas/cli@4.33.1

      - name: Trigger sync fork
        env:
          OSC_ACCESS_TOKEN: unused
          OSC_API_KEY: ${{ secrets.OSC_API_KEY }}
        run: osc --env "${{ inputs.environment }}" admin sync-fork --wait eyevinn-open-live-studio

      - name: Restart olivedev service instances
        if: inputs.environment == 'prod'
        env:
          OSC_ACCESS_TOKEN: unused
          OSC_API_KEY: ${{ secrets.OSC_OLIVEDEV_PAT }}
        run: |
          osc --env prod restart eyevinn-open-live openlivedev
          osc --env prod restart eyevinn-open-live-studio openlivedev
